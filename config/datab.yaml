database:
  mysql:
    host: "localhost"
    port: 3306
    user: "stock_user" # 后续P2阶段我们将创建这个用户
    password: "root1234" # 使用您提供的密码
    database: "stock_database" # 后续创建
    charset: "utf8mb4"
  backup:
    enabled: true
    path: "./data/backup"

  # 连接池配置（可选）
  pool_name: "stock_pool"
  pool_size: 5

  # 其他选项
  charset: "utf8mb4"
  autocommit: true